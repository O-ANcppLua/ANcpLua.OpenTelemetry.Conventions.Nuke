name: CI

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6

      - uses: actions/setup-dotnet@v4
        with:
          global-json-file: global.json

      - name: Restore
        run: dotnet restore OtelConventions.Nuke.slnx

      - name: Build (Release)
        run: dotnet build OtelConventions.Nuke.slnx -c Release --no-restore

      - name: Pack (Release)
        run: dotnet pack src/OtelConventions.Nuke/OtelConventions.Nuke.csproj -c Release --no-build -o artifacts

      - name: Upload .nupkg artifact
        uses: actions/upload-artifact@v4
        with:
          name: nupkg
          path: artifacts/*.nupkg
          retention-days: 30
